# Проверка сборки

## Первый экран с живой машиной — 22 сентября

Фотографическое вступление удалено из обычного режима. На progress=0 canvas видим, первый ракурс совпадает с финальным. Проверен общий план с MEATWASH и записью, переход к кузову и возврат наверх. Фотография загружается только при статическом fallback/reduced-motion; до готовности 3D отображается короткий статус загрузки и работающая запись.

## Оптимизация загрузки — 22 сентября

- GLB: 14 919 340 → 8 937 688 байт, без упрощения геометрии и изменения размера текстур.
- После Meshopt-декодирования проверены все атрибуты вершин и треугольники, включая направление обхода; имена узлов сохранены.
- JavaScript сцены: ~2,19 МБ исходных модулей → ~638 КБ минифицированного bundle с локальным Meshopt-декодером. Это размеры файлов до HTTP-сжатия.
- Два шрифта: 190 988 → 66 848 байт, WOFF2.
- Проверены запуск оптимизированной модели, переход к кузову, отсутствие ошибок в консоли.
- Первый кадр не ждёт 3D; модель загружается параллельно с кодом сцены после первого отображения. Save-Data включает статический режим.
- Локальная подготовка WebGL в проверочном браузере занимает около 18 секунд, включая компиляцию и первый рендер. Это остаётся ограничением тяжёлой сцены; одинаковое ускорение в секундах на любых устройствах не заявляется.

Проверено 21 сентября 2026 во встроенном Chromium, локально, без эмуляции медленной сети.

## Автоматические проверки

`npm run check`: синтаксис ES modules; пути JS/CSS/изображений/шрифтов; локальные якоря; 6 camera stops; 4 услуги; цены из предоставленного JSON; ссылки онлайн-записи обеих площадок. Все проверки проходят.

## Адаптация

Проверены реальные размеры viewport: 1920×1080, 1440×800, 1280×800, 1024×768, 768×1024, 390×760. Нет горизонтального overflow и выхода длинных заголовков за их контейнер. На desktop — верхняя навигация и запись; в portrait 768/390 — меню и нижняя постоянная кнопка.

Первый экран, кузов, салон, полировка, капли, светлая секция, локации, клубный блок и footer просмотрены в браузере. После проверки поправлены крупность кузова, положение машины на mobile, переносы длинных названий, материал сидений и расположение фоновой фотографии гаража.

## Поведение

- Номера глав перемещают к соответствующему ракурсу (невидимый блок миниатюр и прогресса hero удалён 25 сентября).
- «Подробнее» открывает услуги и цены.
- Из окна услуги можно перейти к записи; окно записи предлагает выбрать площадку, и каждая ведёт в свою компанию yclients.
- Телефоны соответствуют предоставленным филиалам.
- «Обсудить условия», «Уточнить стоимость» и «Обсудить уход» открывают контакты администратора и не имитируют создание заявки.
- Меню открывается/закрывается, переходы к секциям работают.
- Native dialog закрывается кнопкой, Escape или нажатием вне окна.
- Статическая версия проверена через `?motion=reduce`; она использует тот же путь, что и системный prefers-reduced-motion, исключая 3D и длинный pinned track.
- Ошибок и предупреждений приложения в консоли при обычной работе не обнаружено.

## Производительность

Начальное синхронное создание WebGL задерживало первое отображение. Исправлено: шрифты и два кадра браузера завершаются до инициализации сцены, компиляция шейдеров асинхронная. После исправления наблюдаемый local LCP — 388 ms. Это локальный замер, не результат Lighthouse на внешнем хостинге или слабом телефоне.

GLB: 14 919 340 байт; все 14 растровых изображений: 1 059 194 байт. Внешний HDR не загружается. DPR ограничен 1.25 desktop / 1 mobile; буфер преломлений — половинное разрешение. Капли instanced, салон объединён по материалам, тень пересчитывается один раз. При неподвижной камере повторный рендер не выполняется.

Во время проверки измеряются median render time и интервал кадров последних 60 обновлений. Значения зависят от ракурса, разрешения и GPU. Гарантия 60 FPS на любом устройстве не заявляется; физический слабый телефон отдельно не тестировался.

## Границы результата

Первый экран и информационные секции используют исходные изображения макета. После вступления работает настоящая 3D-сцена с доступной Porsche 930 Turbo, а не идентичная фотографической машине модель. Геометрия и физическое освещение могут отличаться от фотографий референса. Это явно описано в README, без заявления о пиксельном совпадении всех 3D-ракурсов.

Запись и карты — внешние сервисы. Реальная заявка при проверке не отправлялась; доступность временных слотов внутри Yclients не проверялась. Числа рейтинга/клиентов сохранены из предоставленного макета, а не получены из текущей базы.

## Обновление первого кадра и освещения
Первый кадр заменён на hero-hq.webp (272 526 байт). Экспозиция 0.98, environment intensity 0.88, hemisphere 0.65, key 0.95, fill 0.6, cabin 1.0. Количество источников света, теней и логика камеры не изменены.
# Каталог услуг — 23 сентября 2026

- Сверены 5 программ, 39 услуг и цены четырёх кузовов с сайтом-источником.
- Проверены переключатели седана, кроссовера, внедорожника и микроавтобуса; все пять цен соответствуют матрице.
- Проверены раскрытие состава мойки и категории защиты, диалог записи и реальный адрес Yclients.
- Проверен переход из модального окна полировки к раскрытому разделу полного прайса.
- Встроенный браузер: desktop и 390 × 844; горизонтального переполнения нет. Ошибок и предупреждений в консоли при проверке не зарегистрировано.
- `npm run check`: синтаксис, локальные ресурсы, якоря, наличие всех 39 названий и цен, матрица 20 цен, сохранность шести положений камеры.
- Реальная запись не отправлялась; камера, модель, освещение и bundle в этом обновлении не менялись.

## Фотографии студий

23 сентября: проверены реальные кадры обеих локаций на desktop и 390 × 844, корректная загрузка srcset, мобильное кадрирование, отсутствие горизонтального переполнения и ошибок консоли. Галереи привязаны к соответствующим карточкам Яндекс Карт.

Карточки услуг: проверены новые фотографии на desktop и 390 × 844; srcset выбирает нужный размер, все три изображения загружаются, горизонтального переполнения и ошибок консоли нет. Проверен переход карточки к услуге; модель Porsche не изменялась.

Кадр О клубе: заменён на улучшенный 1448 × 1086, исходник сохранён. Проверены desktop/mobile, загрузка WebP srcset и отсутствие горизонтального переполнения. Светлый переход сужен до 12% справа и 10% снизу на телефоне.

## Технические правки аудита — 25 сентября 2026

- Запись: окно `#booking` предлагает выбрать площадку. Мясницкая ведёт в company/906067, Технопарк — в company/1743348; проверено в Chrome, оба адреса совпадают с данными `meatwash-content.json`. «Записаться» в карточках локаций — прямые ссылки в свою онлайн-запись.
- Контекст записи: кнопки программ и групп прайса несут `data-book-context`; в окне появляется строка «Вы выбрали: …». Проверено на программе «Трёхфазная» и на наборе из примерочной («Мойка кузова · ориентир от 2 150 ₽»).
- «Обсудить условия», «Уточнить стоимость», «Обсудить уход» открывают вариант с телефонами: заголовок из атрибута, текст «Администратор ответит на вопросы и подберёт время», кнопки yclients скрыты.
- Шапка: один писатель состояния. При прокрутке за 70% высоты окна появляются `header.is-solid` и `body.is-past-hero`, при возврате наверх оба снимаются.
- Примерочная: `body.cfg-ready` ставится только после успешной загрузки сцены; в `?motion=reduce` класса нет и кнопка `display:none`. Фокус уходит в панель и возвращается на кнопку, после закрытия скрытые главы и финал снова `inert`.
- Ширины 390 × 844, 768 × 1024, 1440 × 900 в Chrome: `scrollWidth` равен `clientWidth` (390/390, 758/758, 1430/1430), ошибок и предупреждений в консоли нет.
- Тайм-аут загрузки сцены 25 с и `renderer.forceContextLoss()` в `dispose()` — по коду; срабатывание тайм-аута на живой медленной сети не воспроизводилось.
- `scripts/prune-deploy.mjs` проверен на копии `dist` во временной папке: убрано 36 файлов, 19 391 652 байта (исходный GLB, ttf, неиспользуемые логотипы и картинки, `vendor/build`, `vendor/examples`). После отсева битых локальных ссылок в копии нет.
- `npm run catalog`, `npm run check`, `npm run check:bundle` — проходят.
